[project]
name = "tourtoirac"
version = "0.1.0"
description = ""
authors = [
    {name = "MAG\\philippe.chignaguet",email = "philippe.chignaguet@quietamstudio.fr"}
]
requires-python = ">=3.14,<4.0"
dependencies = [
    "autobahn[twisted] (>=26.7.1,<27.0.0)",
    "requests (>=2.34.2,<3.0.0)",
    "coverage (>=7.15.4,<8.0.0)",
    "wheel (>=0.48.0,<0.49.0)",
    "setuptools (>=84.0.0,<85.0.0)",
    "pytest (>=9.1.1,<10.0.0)",
    "websockets (>=17.1,<18.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"


[tool.poetry]
package-mode = false